#drop
  .item-image
    .image-slot
      img(src=item['image'])
    - if (item['sold'])
      .overlay-slot
        img(src="/images/sold_badge.png")

#right-side
  h1= item['title']
  p= item['description']
  p 
    b= "Price asked: "+item['price']+" euro"
  
  .divider3
  .controls
    a(href="/", id="prev") Back to Home